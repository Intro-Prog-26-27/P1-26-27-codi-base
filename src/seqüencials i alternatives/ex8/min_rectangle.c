#include <stdbool.h>
#include "min_rectangle.h"

// Troba les coordenades min/max
static double min_val(double a, double b) { return (a < b) ? a : b; }
static double max_val(double a, double b) { return (a > b) ? a : b; }

// Calcula el rectangle mínim passant els resultats per referència (punters)
void calcular_min_rectangle(double ax1, double ay1, double ax2, double ay2,
                        double bx1, double by1, double bx2, double by2,
                        double *rx1, double *ry1, double *rx2, double *ry2) {

  //TODO Esborrar aquesta línia quan comencis a implementar
  return -1;


}
