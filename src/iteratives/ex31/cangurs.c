/* 31. Dos cangurs avancen simultàniament fent salts sobre una línia recta.
#include "cangurs.h"
El primer cangur parteix de la posició x1 i fa salts de longitud s1.
El segon cangur parteix de la posició x2 i fa salts de longitud s2.

Escriu la funció es_troben(), on x1 i x2 representen les posicions
inicials dels dos cangurs, i s1 i s2 les longituds dels seus salts.
La funció ha de retornar true si, després d'un mateix nombre de salts,
els dos cangurs coincideixen en una mateixa posició, i false en cas
contrari. La funció assumeix que x1 >= 0, x2 >= 0, s1 > 0 i s2 > 0.
(funció a cangurs.c, asserts a main.c)
*/
#include <stdbool.h>

bool es_troben(int x1, int s1, int x2, int s2) {

  //TODO Esborrar aquesta línia quan comencis a implementar
  return false;

}
